// Auto-generated by electrobun-macros
// Do not edit manually

import { join } from "path";
import { dlopen, FFIType, CString } from "bun:ffi";
import type { Window, WindowParams, WindowError, AppError, WebViewError, WebViewOptions, WebViewBounds, FileDialogOptions, FileDialogResult, MessageBoxResult, DialogError, TrayOptions, TrayBounds, TrayError, ClipboardError, SessionError, ShortcutError, DisplayInfo, DisplayError, CoreError, WgpuAdapterOpts, WgpuDeviceOpts, WgpuSurfaceOpts, WgpuError, WindowBounds } from "../api/types";

export interface CoreAPI {
  quitGracefully(): void;
  stopEventLoop(): void;
  forceExit(code: number): void;
  setExitOnLastWindowClosed(value: boolean): void;
  getPlatform(): string;
}

// FFI symbols for CoreAPI
const SYMBOLS = {
  electrobun_core_api_quit_gracefully: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_core_api_stop_event_loop: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_core_api_force_exit: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_core_api_set_exit_on_last_window_closed: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_core_api_get_platform: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
} as const;

export class CoreClient {
  private lib: any;
  private initialized = false;

  constructor(dllPath?: string) {
    const path = dllPath ?? this.findDll();
    this.lib = dlopen(path, SYMBOLS);
    this.initialized = true;
  }

  private findDll(): string {
    const platform = process.platform;
    const suffix = platform === 'win32' ? '.dll' : platform === 'darwin' ? '.dylib' : '.so';
    return join(process.execPath, "..", `electrobun_core${suffix}`);
  }

  private call<T>(method: string, params: any): T {
    const paramsBuf = Buffer.from(JSON.stringify(params), 'utf-8');
    const resultPtr = this.lib.symbols[method](paramsBuf, null);
    if (!resultPtr) return undefined as T;
    try {
      const result = new CString(resultPtr as any).toString();
      if (!result) return undefined as T;
      return JSON.parse(result) as T;
    } catch { return undefined as T; }
  }

  quitGracefully(): void {
    return this.call<void>("electrobun_core_api_quit_gracefully", {});
  }

  stopEventLoop(): void {
    return this.call<void>("electrobun_core_api_stop_event_loop", {});
  }

  forceExit(code: number): void {
    return this.call<void>("electrobun_core_api_force_exit", { code });
  }

  setExitOnLastWindowClosed(value: boolean): void {
    return this.call<void>("electrobun_core_api_set_exit_on_last_window_closed", { value });
  }

  getPlatform(): string {
    return this.call<string>("electrobun_core_api_get_platform", {});
  }

}

export const coreAPI = new CoreClient();
