// Auto-generated by electrobun-macros
// Do not edit manually

import { join } from "path";
import { dlopen, FFIType, CString } from "bun:ffi";
import type { Window, WindowParams, WindowError, AppError, WebViewError, WebViewOptions, WebViewBounds, FileDialogOptions, FileDialogResult, MessageBoxResult, DialogError, TrayOptions, TrayBounds, TrayError, ClipboardError, SessionError, ShortcutError, DisplayInfo, DisplayError, CoreError, WgpuAdapterOpts, WgpuDeviceOpts, WgpuSurfaceOpts, WgpuError, WindowBounds } from "../api/types";

export interface AppAPI {
  getAppName(): string;
  getAppVersion(): string;
  getAppDataPath(): string;
  quit(): void | AppError;
  openExternal(url: string): void | AppError;
  showItemInFolder(path: string): void | AppError;
}

// FFI symbols for AppAPI
const SYMBOLS = {
  electrobun_init_core: { args: [], returns: FFIType.bool },
  electrobun_app_api_get_app_name: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_app_api_get_app_version: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_app_api_get_app_data_path: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_app_api_quit: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_app_api_open_external: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_app_api_show_item_in_folder: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
} as const;

export class AppClient {
  private lib: any;
  private initialized = false;

  constructor(dllPath?: string) {
    const path = dllPath ?? this.findDll();
    this.lib = dlopen(path, SYMBOLS);
    this.initialized = true;
    // 首次加载时自动初始化 core 实例
    try { if (typeof this.lib.symbols.electrobun_init_core === 'function') this.lib.symbols.electrobun_init_core(); } catch {}
  }

  private findDll(): string {
    const platform = process.platform;
    const suffix = platform === 'win32' ? '.dll' : platform === 'darwin' ? '.dylib' : '.so';
    return join(process.execPath, "..", `electrobun_core${suffix}`);
  }

  private call<T>(method: string, params: any): T {
    const paramsBuf = Buffer.from(JSON.stringify(params), 'utf-8');
    const resultPtr = this.lib.symbols[method](paramsBuf, null);
    if (!resultPtr) return undefined as T;
    try {
      const result = new CString(resultPtr as any).toString();
      if (!result) return undefined as T;
      return JSON.parse(result) as T;
    } catch { return undefined as T; }
  }

  getAppName(): string {
    return this.call<string>("electrobun_app_api_get_app_name", {});
  }

  getAppVersion(): string {
    return this.call<string>("electrobun_app_api_get_app_version", {});
  }

  getAppDataPath(): string {
    return this.call<string>("electrobun_app_api_get_app_data_path", {});
  }

  quit(): void | AppError {
    return this.call<void>("electrobun_app_api_quit", {});
  }

  openExternal(url: string): void | AppError {
    return this.call<void>("electrobun_app_api_open_external", url);
  }

  showItemInFolder(path: string): void | AppError {
    return this.call<void>("electrobun_app_api_show_item_in_folder", path);
  }

}

export const appAPI = new AppClient();
