// Auto-generated by electrobun-macros
// Do not edit manually

import { dlopen, FFIType, CString } from "bun:ffi";
import type { Window, WindowParams, WindowError, AppError, WebViewError, WebViewOptions, WebViewBounds, FileDialogOptions, FileDialogResult, MessageBoxResult, DialogError, TrayOptions, TrayBounds, TrayError, ClipboardError, SessionError, ShortcutError, DisplayInfo, DisplayError, CoreError, WgpuAdapterOpts, WgpuDeviceOpts, WgpuSurfaceOpts, WgpuError, WindowBounds } from "../api/types";

export interface SessionAPI {
  getCookies(url: string): string | SessionError;
  setCookie(url: string, name: string, value: string, domain: string | null, path: string | null, secure: boolean, httpOnly: boolean, maxAge: number | null): void | SessionError;
  removeCookie(url: string, name: string): void | SessionError;
  clearCookies(): void | SessionError;
  clearStorageData(): void | SessionError;
}

// FFI symbols for SessionAPI
const SYMBOLS = {
  electrobun_session_api_get_cookies: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_session_api_set_cookie: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_session_api_remove_cookie: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_session_api_clear_cookies: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
  electrobun_session_api_clear_storage_data: { args: [FFIType.ptr, FFIType.ptr], returns: FFIType.ptr },
} as const;

export class SessionClient {
  private lib: any;
  private initialized = false;

  constructor(dllPath?: string) {
    const path = dllPath ?? this.findDll();
    this.lib = dlopen(path, SYMBOLS);
    this.initialized = true;
  }

  private findDll(): string {
    const platform = process.platform;
    const suffix = platform === 'win32' ? '.dll' : platform === 'darwin' ? '.dylib' : '.so';
    return `electrobun_core${suffix}`;
  }

  private call<T>(method: string, params: any): T {
    const paramsBuf = Buffer.from(JSON.stringify(params), 'utf-8');
    const resultPtr = this.lib.symbols[method](paramsBuf, null);
    if (!resultPtr) return undefined as T;
    try {
      const result = new CString(resultPtr as any).toString();
      if (!result) return undefined as T;
      return JSON.parse(result) as T;
    } catch { return undefined as T; }
  }

  getCookies(url: string): string | SessionError {
    return this.call<string>("electrobun_session_api_get_cookies", { url });
  }

  setCookie(url: string, name: string, value: string, domain: string | null, path: string | null, secure: boolean, httpOnly: boolean, maxAge: number | null): void | SessionError {
    return this.call<void>("electrobun_session_api_set_cookie", { url, name, value, domain, path, secure, httpOnly, maxAge });
  }

  removeCookie(url: string, name: string): void | SessionError {
    return this.call<void>("electrobun_session_api_remove_cookie", { url, name });
  }

  clearCookies(): void | SessionError {
    return this.call<void>("electrobun_session_api_clear_cookies", {});
  }

  clearStorageData(): void | SessionError {
    return this.call<void>("electrobun_session_api_clear_storage_data", {});
  }

}

export const sessionAPI = new SessionClient();
