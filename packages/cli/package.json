{
  "name": "@pori15/electrobun-rust",
  "version": "0.1.0",
  "description": "Build ultra fast, tiny, and cross-platform desktop apps with Typescript.",
  "license": "MIT",
  "author": "Blackboard Technologies Inc.",
  "keywords": [
    "bun",
    "desktop",
    "app",
    "cross-platform",
    "typescript"
  ],
  "exports": {
    ".": "./dist/api/bun/index.ts",
    "./bun": "./dist/api/bun/index.ts",
    "./view": "./dist/api/browser/index.ts"
  },
  "type": "module",
  "bin": {
    "electrobun-rust": "./bin/electrobun.ts"
  },
  "homepage": "https://electrobun.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eastgold15/electrobun-rust.git"
  },
  "scripts": {
    "postinstall": "bun scripts/postinstall.ts",
    "start": "bun src/core/bun/index.ts",
    "build:dev": "bun scripts/build-sdk.ts",
    "build:release": "bun scripts/build-sdk.ts --release",
    "dev": "bun scripts/build-sdk.ts",
    "dev:canary": "bun install && bun build:release && cd ../../../apps/kitchen && bun install && bun dev",
    "dev:stable": "bun install && bun build:release && cd ../../../apps/kitchen && bun install && bun dev",
    "build:fast": "bun scripts/build-sdk.ts --npm",
    "build:docs:release": "cd ../../../apps/docs && bun run build",
    "npm:publish": "bun build:release && npm publish",
    "npm:publish:beta": "bun build:release && npm publish --tag beta",
    "typecheck": "bunx tsc --noEmit",
    "lint": "ultracite check src/",
    "lint:fix": "ultracite fix src/",
    "format": "ultracite fix src/",
    "format:check": "ultracite check src/",
    "push:beta": "bun run typecheck && bun scripts/push-version.js beta",
    "push:patch": "bun run typecheck && bun scripts/push-version.js patch",
    "push:minor": "bun run typecheck && bun scripts/push-version.js minor",
    "push:major": "bun run typecheck && bun scripts/push-version.js major",
    "push:stable": "bun run typecheck && bun scripts/push-version.js stable",
    "build:push:artifacts": "bun scripts/build-and-upload-artifacts.js",
    "test": "bun install && bun build:dev && bun build:cli && cd ../../../__tests__ && bun install && bun build:dev && bun start",
    "test:unit": "bun test src/core/shared src/core/bun src/cli"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@babylonjs/core": "^9.10.1",
    "@types/bun": "^1.3.14",
    "png-to-ico": "^3.0.1",
    "rcedit": "^5.0.2",
    "three": "^0.184.0"
  }
}
