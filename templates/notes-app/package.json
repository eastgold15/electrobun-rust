{
	"name": "electrobun-notes-app",
	"version": "1.0.0",
	"type": "module",
	"description": "Electrobun notes app with file system access and RPC",
	"scripts": {
		"start": "electrobun dev",
		"dev": "electrobun dev --watch",
		"build:canary": "electrobun build --env=canary"
	},
	"dependencies": {
		"@pori15/electrobun-rust": "file:../../package"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.7.2"
	}
}
