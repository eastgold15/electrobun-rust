[package]
name = "electrobun-launcher"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "launcher"
path = "main.rs"

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
